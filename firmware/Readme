----------固件请去官网下载--------------------

有最新的尽量用最新固件，历史固件可能有BUG。

--------------------------------------------------


kmboxNet固件20240525_11h37m58s.bin:(建议更新)
    1、更新曲线算法0，bezier非实时，会时间累积。所有点保证准确性。
    2、更新曲线算法1，Arm   非实时，会时间累计。所有点保证准确性。
    3、更新曲线算法2，RT-BeZ实时，不会时间累积。中间点变，最后一次准确。
    4、更新曲线算法3，RT-Arm实时，不会时间累计。中间点变，最后一次准确。
    5、修复屏蔽物理鼠标X,Y后软件移动失效问题。
    6、修复冷启动键鼠失效问题。
    7、修改device口为固定右上鼠标，右下键盘
    8、修改kmbox Net的显示位置
    9、解决键盘卡键问题。
    
出现键鼠数据异常请使用盒子曲线算法。如果要准确性请选择算法0和1.如果要追求瞄准速度请选算法2或者3






kmboxNet固件20240516_17h13m44s.bin（建议更新）
    1、修改device端鼠标特性。
    2、优化解决部分内部bug
    




kmboxNet固件20240424_13h23m15s.bin：
	1、新增带加密功能函数，防止网络抓包特征。
		带enc函数功能说明如下：https://www.bilibili.com/video/BV1YJ4m1H71i
		单机使用推荐带enc前缀的函数



kmboxNet固件20240415_15h51m24s.bin(建议更新)
	1、修改device端键鼠特性。防止检测
	2、支持2种不同的曲线修正切换。(请配合最新上位机程序使用)
		i、 曲线1为贝塞尔曲线算法。
		ii、曲线2为导弹追踪算法。
		后续继续添加更多曲线算法。
	


kmboxNet固件20240315_17h57m48s.bin--（建议更新）
	1、更新硬件曲线修正算法。只影响kmNet_mouse_move函数。
	      如果出现键鼠数据异常，可以开启盒子的硬件曲线修正功能。如果没有异常不用开启 。
	      硬件曲线修正算法适合yolo找到目标，单纯调用kmNet_mouse_move函数，俗称一帧锁的辅助。trace的值略小于一帧视频采样时间。
	      例如，图像采集帧率是每秒60HZ,一帧的时间是16ms，trace的值小于16就会一帧移动到位，如果trace大于16.则在移动过程会被下一帧影响。中途改变向下一个点靠近，保证实时性。
	     
			开启方法见此视频：https://www.bilibili.com/video/BV1Uu4y1u7Qu
			硬件曲线修正效果:https://www.bilibili.com/video/BV1Hx4y1S7dc/

	   ps：软件有自己曲线算法的不用开硬件曲线修正。可能两个叠加后的曲线没有效果。没有移动算法的用盒子算法即可。
	2、新增外接键鼠适配功能。请配合最新上位机程序使用。
	3、增加盒子上电读秒功能。

B+曲线算法同步更新。


kmboxNet固件20231122_22h11m27s.bin
	1、默认隐藏厂商信息，厂商字符串，序列号
	2、新增修改上面信息的接口
	3、新增获取盒子版本号接口，详见getversion函数
	4、每次更新固件后VID和PID都会随机变化一次。


kmboxNet固件20231113_15h48m11s.bin
	1、LCD屏幕上新增版本日期信息，方便查看版本
	2、盒子的Port口限制到1000以上。避免与其他系统端口冲突（部分小于1000端口的如果存在连不上盒子可以升级）
	3、新增恢复出厂设置功能。（长按盒子上的按钮，直到显示屏上闪烁recovery后松开即可恢复出厂设置）

kmboxNet固件20231111_14h4m39s.bin
	1、每个盒子随机VID/PID 。避免被批量针对

kmboxNet固件20231002_17h20m0s.bin
	初始版本